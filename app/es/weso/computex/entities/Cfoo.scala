package es.weso.computex.entities
import com.hp.hpl.jena.rdf.model.Statement

class Cfoo (val statement:Statement){
  
/*  if (resource == null) {
      try {
        val literal = statement.getLiteral()
        
        literal.
      } catch {
        case e: LiteralRequiredException => resource
      }
*/
}